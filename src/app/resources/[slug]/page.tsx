import { notFound } from "next/navigation";
import Link from "next/link";
import { getPost, getResource, resources } from "@/lib/content";

export function generateStaticParams() { return resources.map((resource) => ({ slug: resource.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const resource = getResource((await params).slug); return { title: resource?.title ?? "Resource", description: resource?.description }; }
export default async function ResourcePage({ params }: { params: Promise<{ slug: string }> }) { const resource = getResource((await params).slug); if (!resource) notFound(); const relatedPosts = (resource.relatedPostSlugs ?? []).map(getPost).filter((post): post is NonNullable<ReturnType<typeof getPost>> => Boolean(post)); return <div className="page"><article className="article"><p className="eyebrow">{resource.type} / {resource.access}</p><h1>{resource.title}</h1><p className="lead">{resource.description}</p><div className="article-body"><h2>What it is</h2><p>{resource.description}</p><h2>Who it is for</h2><p>{resource.audience}</p><h2>Why it exists</h2><p>{resource.purpose}</p><h2>How to use it</h2><p>{resource.howToUse}</p><p><a className="arrow-link" href={resource.access === "Free" ? "#download" : "#request"}>{resource.access === "Free" ? "Access the resource ↗" : "Request access ↗"}</a></p>{relatedPosts.length > 0 && <section className="article-links"><p className="eyebrow">Related writing</p>{relatedPosts.map((post) => <Link href={`/blog/${post.slug}`} key={post.slug}>{post.title} ↗</Link>)}</section>}</div></article></div>; }
