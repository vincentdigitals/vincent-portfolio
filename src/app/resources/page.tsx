import Link from "next/link";
import { resources } from "@/lib/content";

export const metadata = { title: "Resources" };
export default function Resources() { return <div className="page"><section className="page-intro"><p className="eyebrow">TOOLS FOR THE WORK</p><h1>Things I build to make the thinking useful.</h1><p>Some of the things I learn will become practical resources — worksheets, checklists, frameworks, guides, and other tools that can help someone work through a problem themselves.</p></section><section className="section"><div className="listing">{resources.map((resource) => <Link className="post" href={`/resources/${resource.slug}`} key={resource.slug}><span className="tag">{resource.type} / {resource.access}</span><h3>{resource.title}</h3><p>{resource.description}</p><span className="meta">TOPIC: {resource.topic}</span></Link>)}{resources.length === 0 && <p className="empty-state">Resources will appear here as practical outputs from the work.</p>}</div></section></div>; }
